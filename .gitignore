node_modules/
dist/
out/
coverage/
.vscode-test/
*.vsix
# Real translation files for trying the extension out (e.g. the data folder of the old app): edu-sharing is GPL, this
# repository Apache-2.0, so they never go in.
/data/
